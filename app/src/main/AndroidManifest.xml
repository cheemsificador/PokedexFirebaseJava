<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pokedex">

        <!-- Actividad de detalles del Pokémon -->
        <activity android:name=".activities.PokemonDetailActivity" android:exported="true" />

        <!-- Pantalla de registro -->
        <activity android:name=".activities.RegisterActivity" android:exported="true" />

        <!-- Pantalla de login -->
        <activity android:name=".activities.LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla principal -->
        <activity android:name=".activities.MainActivity" android:exported="true" />

    </application>

</manifest>
